Socketkit Services - tracking
=============================

Initialize the database:

    psql < db/globals.sql
    psql -1 tracking < db/schema/*

Use "npm" to develop::

    npm install
    npm run dev
