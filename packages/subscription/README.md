Socketkit Services - subscription
=================================

Initialize the database:

    psql < db/globals.sql
    psql -1 subscription < db/schema/*

Use "npm" to develop::

    npm install
    npm run dev
